# Testing Environment Configuration
# This file contains testing-specific settings

ENVIRONMENT=testing
DEBUG=false
LOG_LEVEL=WARNING

# API Configuration
API_HOST=127.0.0.1
API_PORT=8001
API_RELOAD=false

# Testing Security
SECRET_KEY=test-secret-key-for-testing-only
JWT_SECRET_KEY=test-jwt-secret-key-for-testing-only

# Testing Database (separate from development)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=agentic_rag_test
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
DB_ECHO=false

# Testing Vector Database
CHROMADB_HOST=localhost
CHROMADB_PORT=8002
CHROMADB_RFQ_COLLECTION=test_rfq_collection
CHROMADB_OFFER_COLLECTION=test_offer_collection

# Testing Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis
REDIS_DB=1  # Use different database for testing

# Testing Object Storage
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_SECURE=false
MINIO_BUCKET_DOCUMENTS=test-documents
MINIO_BUCKET_THUMBNAILS=test-thumbnails
MINIO_BUCKET_EXPORTS=test-exports

# Testing AI Services (mock or test keys)
OPENAI_API_KEY=test-openai-api-key
OPENAI_ORGANIZATION=test-openai-org
OPENAI_EMBEDDING_MODEL=text-embedding-3-large
OPENAI_CHAT_MODEL=gpt-4-turbo-preview

# Testing Features
ENABLE_DOCS=false
ENABLE_REDOC=false
ENABLE_OPENAPI=false

# Testing CORS (restrictive)
CORS_ORIGINS=["http://localhost:8001"]

# Testing Monitoring
PROMETHEUS_ENABLED=false
OTEL_ENABLED=false

# Testing-specific settings
RATE_LIMIT_REQUESTS=1000  # Higher limit for testing
RATE_LIMIT_WINDOW=60
